# Database
MONGO_URI=mongodb://localhost:27017/belajar_api
MONGO_URI_TEST=mongodb://localhost:27017/belajar_api_test

# Server
PORT=3000

# JWT keys
# For local development you can paste PEM contents on a single line using \n for newlines,
# or store the PEM text in files and set an environment variable pointing to the file contents.
# Example (single-line with \n placeholders):
# JWT_PRIVATE="-----BEGIN RSA PRIVATE KEY-----\nMIIC...\n-----END RSA PRIVATE KEY-----"
# JWT_PUBLIC="-----BEGIN PUBLIC KEY-----\nMIIBI...\n-----END PUBLIC KEY-----"

# If you want to include actual placeholders uncomment below and paste your PEMs as single-line values
# JWT_PRIVATE=-----BEGIN_RSA_PRIVATE_KEY_PLACEHOLDER-----\n...
# JWT_PUBLIC=-----BEGIN_PUBLIC_KEY_PLACEHOLDER-----\n...

# Alternatively, set these as base64-encoded values and decode them in your runtime if you prefer.
# NOTE: Do NOT commit real private keys to source control. Use a secrets manager for production.

# Optional
NODE_ENV=development
